/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["unsplash.com", "images.unsplash.com", "ik.imagekit.io"],
  },
};

module.exports = nextConfig
